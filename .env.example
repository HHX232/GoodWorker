# Postgres, required for Prisma / the whole app.
DATABASE_URL=

# NextAuth session secrets — required for login and every protected API route.
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# AI providers — DeepSeek is preferred when set; OPENROUTER_API_KEY is the
# free-model fallback used when it isn't.
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=

# AI markup percent (src/shared/lib/wallet/pricing.ts) used to be read from
# env here. Now it's admin-editable at runtime, stored in the `WalletSettings`
# DB row (id "global") — set it via the admin panel or GET/PATCH
# /api/admin/wallet-settings, not an env var. Nothing reads AI_MARKUP_PERCENT
# anymore.

# S3-compatible storage (Selectel) — required for uploads (chat attachments,
# etc.) via POST /api/upload. NEXT_PUBLIC_S3_PUBLIC_URL is a separate
# "website" domain on the bucket, not the API endpoint.
S3_REGION=
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=
NEXT_PUBLIC_S3_PUBLIC_URL=
