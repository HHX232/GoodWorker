# Манифест требований

Источник: `2026-09-24-brief.md`. Строку из этого списка может снять **только пользователь**.

> Тикеты 01–04 закоммичены (`7a498fa`, `70b4db4`, `d1412a5`, `2fdcf4c`); прерванное ревью 02–04 доведено облачной сессией — находки закрыты коммитом `8a8e104` (read-модель `GET /library`, загрузка учеником, VIP-гейт на сервере, каскад отзыва, идемпотентный крон). UI 05–07 собран там же, ветка `claude/determined-wozniak-ga0gfz` (от `feature/wallet-balance-topup`). Про чужой код Кошелька в `2fdcf4c` — см. сообщение того коммита.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «очень красивое решение для репетиторов по хранению файлов» | done | расположение: вкладка внутри профиля/дашборда репетитора (ответ пользователя); отдельная страница `/files` (итерация 2 по фидбеку: была вкладкой в дашборде) | T05 |
| R02 | «вложенности файлов» (папки внутри папок) | done | глубина ограничена 6 уровнями (см. G04); коммиты `7a498fa`, `70b4db4` | T01, T02 |
| R03 | «добавление доступа ученикам» | done | доступ выдаётся и на папку целиком, и на отдельный файл; мультивыбор нескольких учеников за один раз; `POST/GET/DELETE /api/tutor-files/grants` + `ShareAccessModal` (мультивыбор) | T03, T06 |
| R04 | «и тому подобное» + референс-скриншот Floe (карточки-папки, светлая палитра, поиск, счётчики, аватарки, боковая навигация) | done | форма карточек/сетки/боковой навигации — из референса, но в палитре/компонентах GoodWorker (см. G06); боковая навигация добавлена после независимой проверки покрытия G2; `FilesShell`: дерево папок слева, крошки, поиск, карточки с аватарами; светлая/тёмная тема | T05 |
| R01i | *(подразумевается)* хранилище бесполезно без загрузки файлов | done | через существующий `uploadFile()`/`app/api/upload`, folder `tutor-files`; коммит `70b4db4` | T02 |
| R02i | *(подразумевается)* вложенность подразумевает создание/переименование/удаление папок | done | создание/переименование/удаление в `FilesShell`, API — `folders/*` | T01, T02, T05 |
| R03i | *(подразумевается)* доступ ученику подразумевает просмотр/скачивание им файлов на своей стороне | done | ученик видит расшаренное и в отдельной вкладке своего дашборда, и в чате с репетитором (ответ: «И там, и там»); `/files` у ученика — «Доступно мне» (группы по репетитору) + карточка `FILE_ACCESS_GRANTED` в чате | T07 |
| R04i | *(подразумевается)* «добавление доступа» подразумевает и его отзыв | done | отзыв в `ShareAccessModal`; отзыв папки снимает и гранты на личные подпапки ученика в этой ветке | T03, T06 |
| R05i | *(подразумевается)* доступ выдаётся только связанным ученикам репетитора (`TeacherStudent`), не любому пользователю | done | копирует паттерн `hasTeacherStudentLink` из `chat/access.ts`; коммиты `7a498fa`, `d1412a5` | T01, T03 |
| R06i | *(подразумевается из скриншота)* поиск по файлам/папкам | done | глобальный поиск сверху по всей библиотеке (не только текущая папка); глобальный поиск в тулбаре `FilesShell` (`GET /search`) | T03, T05 |
| R06i.1 | ученик ищет только среди расшаренного ему | done | `loadStudentVisibility()` — общий фильтр для поиска и библиотеки | T03, T07 |
| R07i | *(подразумевается)* хранение файлов — через уже существующий S3-аплоадер проекта, не новый бэкенд | done | подтверждено — `uploadFile()`/`app/api/upload`, никаких изменений в апло­адере; коммит `70b4db4` | T02 |
| R08i | *(подразумевается)* удаление файла/папки репетитором | done | удаление сразу (без корзины), при удалении папки с расшаренными файлами доступ отзывается каскадно автоматически (ответ); удаление с подтверждением; каскад через FK | T02, T05 |
| G01 | «Только VIP» — фича только для VIP-репетиторов | done | не-VIP репетитор видит заглушку-апселл с предложением оформить VIP вместо содержимого раздела (ответ); заглушка-апсейл в `FilesShell` + 403 `VIP_REQUIRED` на создание/загрузку/выдачу доступа | T05 |
| G02 | «лимит фиксированный, если превышает лимит то идет снятие денег со счета (отдельная ветка где vip идет за пополнение счета)» | done (цена — placeholder) | лимит 7 ГБ на VIP-репетитора; ежемесячная подписка на перелимит через Кошелёк, цена — placeholder (см. «Открытые места» в спецификации); `StorageOverageWarningModal` при первом переходе за 7 ГБ; крон идемпотентен по месяцу; цена — заглушка `0` | T01, T04, T06 |
| G03 | «можно [ученику загружать], на это отдельный вопрос при создании куда может загружать ученик файлы. Эт типо как класс заливает файлы свои у каждого своя папка и своя проверка репетитором» | done | у папки — флаг «ученики могут загружать»; когда включён, каждый ученик с доступом получает свою приватную подпапку для сдачи файлов (видна только ему и репетитору), репетитор проверяет; тумблер в `ShareAccessModal`; подпапки создаются и для унаследованного доступа (`ensureSubfoldersForGrant`) | T01, T03, T06, T07 |
| G04 | глубина вложенности папок | done | ограничена 6 уровнями (ответ); коммиты `7a498fa`, `70b4db4` | T01, T02 |
| G05 | предпросмотр файлов | done | встроенный просмотр PDF и изображений внутри интерфейса, не только скачивание (ответ); `FilePreviewModal` (img/iframe), прочие типы — скачивание | T05 |
| G06 | визуальный язык раздела | done | форма карточек/сетки — из референса Floe, но в палитре и компонентах GoodWorker (см. R04); `src/widgets/Files/*` — SCSS-модули, палитра GoodWorker, блок тёмной темы; дерево сворачивается в выпадающее меню при ширине контейнера <700px | T05 |
